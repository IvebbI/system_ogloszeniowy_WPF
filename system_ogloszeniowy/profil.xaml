<Window x:Class="system_ogloszeniowy.profil"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:system_ogloszeniowy"
        mc:Ignorable="d"
        Title="profil" Height="750" Width="1200">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Górny pasek z logiem -->
        <StackPanel Orientation="Horizontal" Background="#2196F3" VerticalAlignment="Center" HorizontalAlignment="Left">
            <Image Source="logo.jpg"/>
        </StackPanel>

        <!-- Panel nawigacyjny -->
        <Grid Grid.Row="0" Margin="0,60,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
                <Button Content="Główna" Margin="5" Padding="10,5"/>
                <Button Content="Ogłoszenia o pracę" Margin="5" Padding="10,5"/>
                <Button Content="Moje Konto" Margin="5" Padding="10,5"/>
                <Button Content="Dodaj Ogłoszenie" Margin="5" Padding="10,5"/>
            </StackPanel>
        </Grid>

        <!-- Zawartość główna -->
        <Grid Grid.Row="1">
            <Label Content="ss"/>
        </Grid>
    </Grid>
</Window>
